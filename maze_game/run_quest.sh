#!/bin/bash

# Run the Quest Maze Adventure

# Navigate to the project directory
cd "$(dirname "$0")"

# Make sure the script is executable
chmod +x quest_terminal_interface.py

# Run the quest terminal interface
python quest_terminal_interface.py
