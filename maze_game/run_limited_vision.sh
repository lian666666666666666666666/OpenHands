#!/bin/bash

# Run the Limited Vision Maze Adventure

# Navigate to the project directory
cd "$(dirname "$0")"

# Make sure the script is executable
chmod +x limited_vision_interface.py

# Run the limited vision interface
python limited_vision_interface.py
